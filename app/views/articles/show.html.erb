<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2>Commets</h2>
<% @article.commets.each do |comment| %>
  <p>
  	<strong>Commenter</strong>
  	<%= comment.commenter %>
  </p>

  <p>
  	<strong>Comment:</strong>
  	<%= cmment.body %>
  </p>
<% end %> 
 
<h2>Add a comment</h2>
<%= form_for ([@article, @article.comments.build]) do |f| %>
  <p>
  	<%= f.label :commenter %><br>
  	<%= f.label_field :commenter %>
  </p>
  <p>
  	<%= f.label :body %><br>
  	<%= f.text_area :boody %>
  </p>
  <p>
  	<%= f.sumbit %>
  </p>
<% end %>  


<%= link_to 'Back', articles_path %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>